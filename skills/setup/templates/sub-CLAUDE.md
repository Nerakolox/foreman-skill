<!-- foreman 模板：子仓 CLAUDE.md。尖括号是占位，全部换成真实内容。<总管> 是本文件到总管目录的相对路径：A 方案一般是 `..`，monorepo 按层级算（`apps/web/` 是 `../..`）；B 方案是 `../<总管目录>`，子仓不在总管旁边的按实际位置算。所有路径都写相对本文件的路径，不写绝对路径。生成时删掉所有注释。整份不超过 200 行；放不下的长规范拆进 .agents/rules/。 -->
# <子仓名>：<一句话职责>

<!-- B 方案才保留下面这一节，A 方案删掉。 -->
## 启动

本仓和总管平级，要这样启动才能读到总管规则和 spec：

```bash
CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1 claude --add-dir <总管>
```

开工前自检：上下文里如果没有总管的 CLAUDE.md（《<项目名>》那份），说明启动方式不对，停下来让用户用上面的命令重启，不要凭记忆干活。

## 总管文档（不在本仓内）

| 什么 | 路径 | 什么时候读 |
| --- | --- | --- |
| 总管规则 | `<总管>/CLAUDE.md` | Claude Code 启动时自动加载，不用再读；用别的 AI 工具时，开工前先读 |
| 契约索引 | `<总管>/spec/INDEX.md` | 开工之前，找到这次涉及的章节 |
| 任务台账 | `<总管>/plan/tasks.md` | 开工时、做完时，改自己那一行的状态 |
| 联调任务单 | `<总管>/joint-tasks/` | 这次的活来自任务单时 |
| 总管细则 | `<总管>/.agents/rules/` | 按总管 CLAUDE.md「细则」表的时机 |

## 边界

1. 本仓只做<职责>。<其他子仓>的代码一行不碰；需要它们配合，停下来告诉用户，由包工头派活或开任务单。
2. 开工之前，先读契约索引，把这次涉及的字段、接口、错误码吃透。
3. 本地实现和 spec 对不上，停下来对齐，不许边写边改。
4. 每个活都要在任务台账里有一行：有就开工时改成 `开发中`，没有就先加一行；做完改状态。
5. 活来自联调任务单时，先读那份任务单，只勾本仓那一栏。

## 红线

spec 里没有的字段、接口、错误码，一个都不许引进来。确实需要，先提出来，等 spec 改好再写。

## 谁说了算

`spec/` → 总管 `CLAUDE.md` 和总管细则 → 本文件 → 本仓细则 → 代码。代码和 spec 冲突，改代码。

## 常用命令

<!-- 只写从 package.json、Makefile 等处核实过的命令。没有就删掉这一节。 -->
- <安装依赖>
- <本地运行>
- <测试>

## 细则

<!-- 每个细则文件一行，写死读取时机。不要用 @ 引用，@ 会在启动时把整份文件读进来。 -->
细则不会自动加载，到了「什么时候读」那一列的时机，再去读对应的文件。路径相对本文件。

| 主题 | 路径 | 什么时候读 |
| --- | --- | --- |
| <组件设计> | `.agents/rules/<component-design>.md` | <新建、拆分组件之前> |
| <代码风格> | `.agents/rules/<code-style>.md` | <写或改代码之前> |
