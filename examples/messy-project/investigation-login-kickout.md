# 排查：登录后偶尔被踢回登录页

## 现象

app 登录后，用一会儿就被踢回登录页，但 token 其实没过期。

## 排查

- 抓包看到后端返回 code 40101
- app 判断未登录用的是 401001，对不上，走了兜底逻辑，兜底逻辑直接清 token
- 后端 token 过期返回 40101，app 以为是未知错误

## 结论

app 的错误码写错了，应该是 40101。先在 app 里临时兼容两个值，回头统一。

## 状态

临时兼容了，没彻底改。
